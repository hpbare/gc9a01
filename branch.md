## Branches description

| Branch | Description |
|---|---|
| `main` | Main driver library. |
| `driver-async-blocking` | Developing async spi transmit mechanism, will be integrated to driver. |
| `example-async-blocking` | Testing and demo for `driver-async-blocking`. |
| `driver-blocking` | Only blocking mechanism. |
| `example-blocking` | Testing and demo for `driver-blocking`. |
| `docs-branch` | A chore branch that help me works with these dumps. |

> Note: This branch will be deleted when the driver is completed.